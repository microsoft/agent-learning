[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "azure-agents-learning-sdk"
version = "0.2.0"
description = "Native reinforcement learning SDK for AI agents using Azure AI Evaluation judge metrics."
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [{ name = "CMS Top Customers" }]
keywords = ["reinforcement-learning", "ai-agents", "azure", "evaluation", "rlhf"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "numpy>=1.24",
    "azure-identity>=1.15",
    "azure-cosmos>=4.5",
    "azure-ai-evaluation>=1.0.0",
    "pydantic>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4",
    "pytest-asyncio>=0.21",
    "pytest-cov>=4.1",
    "ruff>=0.1",
    "mypy>=1.6",
]
nlp = [
    "scikit-learn>=1.3",
    "scipy>=1.10",
    "joblib>=1.3",
]
slm = [
    "onnxruntime-genai>=0.5",
]
llm = [
    "azure-ai-evaluation>=1.0.0",
    "azure-identity>=1.15",
]

[project.scripts]
agent-learn = "agent_learning.cli:main"

[project.urls]
Homepage = "https://github.com/microsoft/azure-agents-learning-sdk"
Repository = "https://github.com/microsoft/azure-agents-learning-sdk"
Issues = "https://github.com/microsoft/azure-agents-learning-sdk/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
agent_learning = ["py.typed"]

[tool.ruff]
line-length = 110
target-version = "py310"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
